# WSL

# wsl shutdown
alias ws='wsl --shutdown'

# Git

# Comando para git status
alias gs='git status'

# Comando para git add .
alias gadd='git add .'

# Comando para git commit 
# alias gc='git commit -m '

# Comando para git add + git commit
alias gc='git add . && git commit -m '

# Comando para git push
alias gp='git push'

# Comando para git pull
alias gpll='git pull'

# Comando para git checkout
alias gck='git checkout'

# Comando para git checkout -b
alias gcb='git checkout -b'

# Comando para git branch
alias gb='git branch'

# Comando para git branch -d
alias gbd='git branch -d'

# Acesso a diretorios

# Comando para entrar no dir ws-intellij
alias cdv='cd c:/dev/projects/ws-intellij'

# Comando para entrar no dir ws Kotlin
alias cdk='cd c:/dev/projects/ws-kotlin'

# Docker e Docker-compose

# Comando para docker ps 
alias dps='docker ps'

# Comando para docker-compose up -d
alias dcud='docker-compose up -d'

# Comando para docker-compose stop
alias dcs='docker-compose stop'

# Comando para docker-compose down
alias dcd='docker-compose down'

# Comando para docker-compose ps 
alias dcps='docker-compose ps'

# Kubernetes

# Comando para kubectl
alias k='kubectl'

# Kafka windows

# Comando para rodar o kafka no windows
# alias zpr="./bin/windows/zookeeper-server-start.bat ./config/zookeeper.properties"
# alias kr="./bin/windows/kafka-server-start.bat ./config/server.properties"